http_server:
  port: 9799
  listen: 0.0.0.0
  log_file: /var/log/grommunio_exporter.log
  # Optional api authentication
  no_auth: true
  username:
  password:
grommunio:
  # Mysql settings, default read from /etc/gromox/mysql_adaptor.cfg
  mysql_username: 
  mysql_password: 
  mysql_database: 
  mysql_host: 
  mysql_port: 3306
  # Optional overrides
  cli_binary:
  gromox_binary:
  alternative_hostname: